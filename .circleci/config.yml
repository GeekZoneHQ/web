version: 2.1

# Define the jobs we want to run for this project
jobs:
  build:
    docker:
      - image: 'geekzone/infra'
    environment:
      TAG: 0.1.<< pipeline.number >>
    steps:
      - checkout
      - run: 
          name: show variable
          command: |
            echo $TAG